<mp-toptips msg="{{errorMsg}}" type="error" show="{{errorMsg}}" delay="1500"></mp-toptips>

<block wx:if="{{userRole == 1}}">
    <patientUpdateInfo patientInfo="{{roleInfo}}" patientRules="{{patientRules}}"></patientUpdateInfo>
</block>
<block wx:elif="{{userRole == 2}}">
   <doctorUpdateInfo doctorInfo="{{roleInfo}}" doctorRules="{{doctorRules}}"></doctorUpdateInfo>
</block>